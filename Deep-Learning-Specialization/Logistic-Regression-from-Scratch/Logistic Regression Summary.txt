1. Load the Data
2. Preprocess the Data
   a. See the dimensions of the images
   b. Reshape the Images to 1-D arrays
   c. Initialize the images(/ every pixel by 255.)
3. Build the helper functions
   a. sigmoid(z)
   b. initialize(dim)
   c. propagate(w, b, X, Y)
   d. optimize(w, b, X, Y, num_iterations, learning_rate, print_cost)
   e. predict(w, b, X)
4. Build a model Function
   f. model(X_train, Y_train, X_test, Y_test, num_iterations, learning_rate, print_cost)
5. Call the model function through the loaded dataset
